#!/bin/sh

# Copyright (C) 2013 Jonathan Geisler

$user=`whoami`

if [ `ls /home/$user/active-contests | wc -l` -gt 0 ] ; then
	for file in /home/$user/active-contests/*
	do
		dir=`basename $file`
		cd "/home/$user/public_html/$dir/judge"
		./cronScript.php > ./errorLog.txt &
	done
fi
